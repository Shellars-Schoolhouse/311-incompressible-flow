%Written by Sean Hellar, 5 FEB 2022
%Homework 2 questions 

% Note: @ symbolizes infinite

% Problem 1 
% Given 
    % Temp@ = 245 K 
    % P@ = 4.35e4 N/m^2
    % P = 3.5e4 N/m^2 
    % r = 1.4 
    % R = 287
% Find density at point in isentropic flow
    % rho@ = (P@ / (R*Temp@) ) 
    %%% rho = ( (P/P@)^(1/r) ) * rho@ 
    
    

% Problem 2 
% Given
    % Temp0 = 960 K 
    % Temp_ex = 600 K
    % C_p = 1004.5 
% Find velocity at exit of nozzle 
   %%% V = sqrt( 2 * C_p * (Temp0-Temp_ex) )
   
   
   
% Problem 3 
% Given 
    % P@ = .61 atm 
    % rho@ = .819 kg/m^3
    % V@ = 300 m/s 
    % P = .5 atm 
    % r = 1.4 
    % R = 287
    % C_p = 1004.5
%Calculate velocity at point 
    % Temp@ = ( P@ / (rho@*R) )
    % Temp = (P/P@)^(r-1/r) * Temp@
    %%% V = sqrt( 2 * C_p * (Temp@-Temp) + V@^2 ) 
    
    
    
% Problem 4 
% Given 
    % P@ = .61 atm 
    % rho@ = .819 kg/m^3
    % V@ = 300 m/s 
    % P = .47 atm 
    % r = 1.4 
    % R = 287
    % C_p = 1004.5
%Calculate velocity correctly then use bernouillis  
    % Temp@ = ( P@ / (rho@*R) )
    % Temp = (P/P@)^(r-1/r) * Temp@
    %%% V = sqrt( 2 * C_p * (Temp@-Temp) + V@^2 ) 
% Bernouillis 
    %%% V = sqrt( ( (2*(P@-P))/rho@ ) + V@^2
    
    
    
    
% Problem 5 
% Given 
    % Temp0 = 519 R
    % V@ = 1340 ft/s
    % C_p = 6006 
    % r = 1.4
    % R = 1716 
% Find test section Mach number 
    % Temp = Temp0 - ( V@^2 / (2*C_p) )
    % a = sqrt( r * R * Temp )
    %%% M = V@ / a
    





